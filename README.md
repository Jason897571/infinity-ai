# Infinity AI - 无限运行的AI Agent框架

基于Anthropic的["Effective Harnesses for Long-Running Agents"](https://www.anthropic.com/engineering/effective-harnesses-for-long-running-agents)设计理念构建的AI Agent自动化框架。

## 🎯 核心理念

解决AI Agent跨多个上下文窗口保持一致进度的挑战。每个新会话都像"轮班的工程师，新来的工程师对上一班发生的事情毫无记忆"。

本框架通过**持久化状态工具包**让新会话能够快速理解已完成的工作和剩余任务。

## ✨ 特性

- 🔄 **无限运行** - Agent可以跨多个会话持续工作，直到任务完成
- 📋 **智能任务管理** - 基于优先级的任务调度和追踪
- 🧪 **自动化测试** - 使用浏览器自动化验证功能实现
- 📝 **详细日志** - 完整的进度追踪和会话记录
- 🔐 **Git集成** - 自动提交和版本控制
- 🎨 **两种Agent模式**:
  - **Initializer Agent**: 初始化项目环境，生成功能列表
  - **Coding Agent**: 增量实现功能，测试验证

## 🚀 快速开始

### 安装

```bash
# 克隆仓库
git clone https://github.com/jasongu/infinity-ai.git
cd infinity-ai

# 安装依赖
pip install -e .
```

### 配置

设置Claude API密钥：

```bash
export ANTHROPIC_API_KEY="your-api-key-here"
```

### 使用

#### 1. 初始化项目

创建项目需求文件 `requirements.txt`：

```
构建一个简单的待办事项Web应用：
- 用户可以添加新任务
- 任务可以标记为完成
- 支持删除任务
- 任务列表持久化保存
```

运行初始化：

```bash
infinity-ai init --requirements requirements.txt
```

这将创建：
- `feature_list.json` - 功能列表
- `claude-progress.txt` - 进度日志
- `init.sh` - 开发服务器启动脚本

#### 2. 运行Agent

```bash
# 连续运行模式（推荐）
infinity-ai run --mode continuous

# 单次运行模式
infinity-ai run --mode single

# 交互模式（每次会话后暂停）
infinity-ai run --mode interactive
```

#### 3. 查看状态

```bash
infinity-ai status
```

输出示例：
```
============================================================
INFINITY AI - PROJECT STATUS
============================================================

Project Root: /path/to/project

📊 Features:
  Total:      12
  Completed:  8
  Pending:    4
  Progress:   66.7%

📈 Sessions:
  Total Sessions: 15
  Features Completed: 8

🎯 Next Feature:
  ID: feature_009
  Description: 用户可以删除任务
  Priority: 2

============================================================
```

## 📖 命令参考

### `infinity-ai init`

初始化项目，创建必要的文件结构。

**选项：**
- `-p, --project-root` - 项目根目录（默认：当前目录）
- `-r, --requirements` - 项目需求描述文件（必需）
- `-c, --config` - 配置文件路径

### `infinity-ai run`

运行AI Agent自动完成任务。

**选项：**
- `-p, --project-root` - 项目根目录
- `-m, --mode` - 运行模式：`continuous`、`single`、`interactive`
- `-c, --config` - 配置文件路径

### `infinity-ai status`

查看项目当前状态。

### `infinity-ai report`

生成项目报告。

**选项：**
- `-o, --output` - 输出文件名（默认：feature_report.md）

### `infinity-ai add-feature`

手动添加功能到列表。

```bash
infinity-ai add-feature "用户登录功能" \
  --steps "Navigate to login page" \
  --steps "Enter credentials" \
  --steps "Click login button" \
  --steps "Verify user logged in" \
  --category functional \
  --priority 1
```

### `infinity-ai complete-feature`

手动标记功能为完成。

```bash
infinity-ai complete-feature feature_001
```

## 🏗️ 架构

### 核心组件

```
infinity_ai/
├── core/                  # 核心模块
│   ├── initializer.py     # 初始化Agent
│   ├── coding_agent.py    # 编码Agent
│   ├── scheduler.py       # 调度器
│   ├── feature_manager.py # 功能管理器
│   └── progress_tracker.py# 进度追踪器
├── config/               # 配置模块
│   ├── settings.py       # 全局设置
│   └── llm_config.py     # LLM配置
├── utils/                # 工具模块
│   ├── git_utils.py      # Git管理
│   ├── file_manager.py   # 文件管理
│   ├── browser_tester.py # 浏览器测试
│   └── logger.py         # 日志系统
└── cli.py               # 命令行接口
```

### 工作流程

```
┌─────────────────────────────────────────────────────┐
│                 第一次会话                           │
│  Initializer Agent                                   │
│  1. 分析项目需求                                      │
│  2. 创建功能列表 (feature_list.json)                  │
│  3. 创建启动脚本 (init.sh)                           │
│  4. 初始化Git仓库                                     │
└─────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────┐
│               后续会话（循环）                         │
│  Coding Agent                                        │
│  1. 读取进度文件和Git日志                             │
│  2. 选择下一个未完成的功能                            │
│  3. 实现功能代码                                      │
│  4. 运行测试验证                                      │
│  5. 提交代码并更新进度                                │
└─────────────────────────────────────────────────────┘
```

## 🔧 配置

### 环境变量

- `ANTHROPIC_API_KEY` - Claude API密钥（必需）

### 配置文件

可以创建 `config.json` 自定义配置：

```json
{
  "max_context_windows": 100,
  "max_retries": 3,
  "browser_headless": true,
  "test_timeout": 30000,
  "auto_commit": true
}
```

## 🎓 设计原则

### 四大失败模式及解决方案

| 问题 | 解决方案 |
|------|---------|
| 过早声称"完成" | 综合功能列表强制系统化完成 |
| Bug/未记录进度 | Git + 进度文件提供双重历史 |
| 未测试就标记完成 | 明确测试步骤 + 验证要求 |
| 花时间弄清如何运行 | `init.sh` 提供即时启动 |

### 关键设计决策

1. **功能列表使用JSON格式** - 比Markdown更难被模型意外修改
2. **结构化测试步骤** - 每个功能包含明确的验证步骤
3. **浏览器自动化测试** - 像用户一样测试，确保功能真正可用
4. **持久化状态** - 新会话通过3个文件快速理解项目状态

## 🧪 测试

```bash
# 安装开发依赖
pip install -e ".[dev]"

# 运行测试
pytest

# 代码覆盖率
pytest --cov=infinity_ai
```

## 📝 示例

查看 `examples/` 目录获取更多使用示例：

- `examples/simple-web-app/` - 简单Web应用示例
- `examples/todo-app/` - 待办事项应用示例

## 🤝 贡献

欢迎贡献！请查看 [CONTRIBUTING.md](CONTRIBUTING.md) 了解详情。

## 📄 许可证

MIT License - 详见 [LICENSE](LICENSE) 文件

## 🙏 致谢

- 基于 [Anthropic的工程博客](https://www.anthropic.com/engineering/effective-harnesses-for-long-running-agents)设计理念
- 使用 [Claude API](https://www.anthropic.com/) 提供AI能力

## 📚 相关资源

- [Effective Harnesses for Long-Running Agents](https://www.anthropic.com/engineering/effective-harnesses-for-long-running-agents) - Anthropic工程博客
- [Claude Agent SDK](https://platform.claude.com/docs/en/agent-sdk/overview) - 官方Agent SDK文档
- [Quickstart Examples](https://github.com/anthropics/claude-quickstarts/tree/main/autonomous-coding) - 官方快速开始示例